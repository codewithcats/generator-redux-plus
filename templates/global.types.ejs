// Auto-generated by the generator
// Don't manually edit this file

// @flow

<% for (s in states) { -%>
import type {<%= s.type %>} from './<%= s.name %>'
<% } -%>

export type Action<P> = {
  type: string,
  payload: P
}

export type Reducer<S, P> = (state: S, action: Action<P>) => S

export type Effect = Generator<any, any, any>

export type ApplicationState = {
<% for (s in states) { -%>
  <%= s.name %>: <%= s.type %>,
<% } -%>
}
